<?xml version="1.0" encoding="utf-8"?>
<selector xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Stato quando il pulsante è selezionato -->
    <item android:state_checked="true" android:color="@color/selectedColor" />

    <!-- Stato quando il pulsante non è selezionato -->
    <item android:color="@color/unselectedColor" />
</selector>